// 삼항 연산자
#include <stdio.h>

int main(){
	
	// 참(0이 아닌것), 거짓(0)
	// 조건식?참:거짓;
	// 조건식이 참이면 참을 수행 거짓이면 거짓을 수행
	// if문을 간단히 쓴다라고 생각하면 된다.
	// 삼항 연산자도 이중if문 처럼 쓸 쑤있는데 그럴바엔 이중 if문이나 if-else문을 쓰자.
	(3) ? printf("3은 True\n") : printf("3은 False\n");

	return 0;

}
